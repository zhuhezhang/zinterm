export struct OutputRuleItem {
    name: string,
    pattern: string,
    regex: bool,
    case-sensitive: bool,
    whole-line: bool,
    color: string,
    swatch: color,
    enabled: bool,
}

export struct AlgorithmOptionRow {
    name: string,
    checked: bool,
    weak: bool,
    can-move-up: bool,
    can-move-down: bool,
}
